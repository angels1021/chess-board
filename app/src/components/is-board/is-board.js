(function(){
    "use strict";

    class isBoardCtrl{
        constructor(){
            this.hrz = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h'];
            this.vert = [1, 2, 3, 4, 5, 6, 7, 8]
        }
    }

    angular.module('isChess').component('isBoard', {

    });
})();